import { Field } from '../../../lib/data/Field';
declare type Options = {
    dbOptions: object;
    gqlOptions: object;
};
declare const _default: ({ options }: Field, type: string) => Options;
export default _default;
