import {
Resolver,
Query,
Args,
{{!-- {{#if (length entity.relations.toMany)}}
  ResolveField,
  Parent
{{/if}} --}}
} from '@nestjs/graphql'
import {
{{entity.name}},
}
from '{{relativeImport ($get 'entity.resolverPath') ($get 'entity.entityPath')}}'
