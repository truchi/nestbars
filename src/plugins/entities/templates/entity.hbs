{{$set 'entity' (call entity 'data')}}

{{~>banner~}}

{{>imports}}

{{#each ($get 'entity.enums') as |enum|}}
  {{>enum enum=enum}}

{{/each}}

{{>entity}}

{{#each ($get 'entity.enums') as |enum|}}
  {{>register enum=enum}}

{{/each}}
