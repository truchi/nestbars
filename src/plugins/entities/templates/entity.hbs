{{$set 'entity' (call entity 'data')}}

{{~>banner~}}

{{>imports}}

{{>type__primary}}

{{>type__data}}

{{#each entity.enums as |enum|}}
  {{>enum enum=enum}}

{{/each}}

{{>entity}}

{{#each entity.enums as |enum|}}
  {{>register enum=enum}}

{{/each}}
