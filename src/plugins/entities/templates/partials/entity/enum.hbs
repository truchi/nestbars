export enum {{enum.options.options.name}} {
{{#each enum.options.options.values as |value|}}
  {{value}} = '{{value}}',
{{/each}}
}

registerEnumType({{enum.options.options.name}}, {
name: '{{enum.options.options.name}}',
{{#if enum.options.options.description}}
  description: '{{enum.options.options.description}}',
{{/if}}
})
