async update(
args: Update{{entity.name}}Args,
): Promise<{{entity.name}}> {
  const primary = args.primary()
  const data = args.data()

  await this.repository.update(primary, data)
  return this.get(primary)
  }
