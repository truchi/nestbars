import { Injectable } from '@nestjs/common'
import { InjectRepository } from '@nestjs/typeorm'
import { Repository } from 'typeorm'
import {
{{#each ($get 'entity.dependencies') as |dependency|}}
  {{dependency}},
{{/each}}
} from '{{relativeImport ($get 'entity.serviceDest') ($get 'entity.entityDest')}}'
