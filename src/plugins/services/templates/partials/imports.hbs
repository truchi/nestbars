import { Injectable } from '@nestjs/common'
import { InjectRepository } from '@nestjs/typeorm'
import { Repository } from 'typeorm'
import {
{{entity.name}},
{{#each entity.enums as |enum|}}
  {{enum.options.name}},
{{/each}}
}
from '{{relativeImport ($get 'entity.servicePath') ($get 'entity.entityPath')}}'
