{{$set 'options' ($call entity 'dbOptions')}}

@Entity(
{{#if entity.options.name}}
  '{{entity.options.name}}',
{{/if}}
{{#unless ($is_empty ($get 'options'))}}
  {{$stringify ($get 'options')}}
{{/unless}}
)

{{$delete 'options'}}
