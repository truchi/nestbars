{{>banner~}}

{{>entity__imports}}

{{#each (enums entity) as |enum|}}
  {{>entity__enum .. enum=enum}}

{{/each}}

{{>entity__db_decorator}}
{{>entity__gql_decorator}}

export class {{entity.name}} {
{{#each entity.fields as |field|}}
  {{>entity__field__field .. field=field}}

{{/each}}
}
