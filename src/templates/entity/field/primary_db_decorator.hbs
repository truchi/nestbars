{{$set 'options' ($call field 'dbOptions')}}

@PrimaryGeneratedColumn(
{{#switch field.type}}
  {{#case 'uuid'}}
    'uuid',
  {{/case}}
{{/switch}}
{{#unless ($is_empty ($get 'options'))}}
  {{$stringify ($get 'options')}}
{{/unless}}
)

{{$delete 'options'}}
