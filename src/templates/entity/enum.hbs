export enum {{enum.tsName}} {
{{#each enum.values as |value|}}
  {{value}} = '{{value}}',
{{/each}}
}

registerEnumType({{enum.tsName}}, {
name: '{{enum.tsName}}',
{{#if enum.options.description}}
  description: '{{enum.options.description}}',
{{/if}}
})
